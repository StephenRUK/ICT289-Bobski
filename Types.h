#ifndef CUSTOM_TYPES_H
#define CUSTOM_TYPES_H

typedef float vec2[2];
typedef float vec3[3];

typedef float vertex2[2];		// Use for texture coordinates
typedef float vertex3[3];		// Use for vertices, normals

#endif
